# 二叉树的最大深度
力扣104
## 初步思路

这题就是一直往左右遍历，如果不是None，每往左走一次或者往右走一次，深度就+1

```python 
    def maxDepth(self, root: Optional[TreeNode]) -> int:
        if not root:
            return 0
        return max(self.maxDepth(root.left)+1,self.maxDepth(root.right)+1)

```

意思就是，不断往下走，走一下加一下1就行