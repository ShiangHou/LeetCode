# 将有序数组转化为二叉搜索树

力扣108

## 思路
这个跟反序列化的那个还是很像的

我们都知道，二叉搜索树按照中序遍历就是一个递增的数组，那么这里就是要反序列化
偷偷看了一眼，其实就是反着来就行

不对，之前我们在写反序列化的时候，是有None的，现在没有None了，那咋办，那我直接按照原来的build写不就写成一个链表了嘛

看了一下题目，题目要求我们最后生成一个**平衡二叉搜索树**

那么就是其实 emmmmm，左右不能太离谱，相差不能是大于1，

搜嘎，直接在数组里面求中间值，用中间值去构建root，然后左边就递归调用左边的数组嘛，右边也递归调用嘛，记得传变量的时候数组更新一下就行，

这其实跟那个什么从后序中序推出二叉树差不多


```python 
    def sortedArrayToBST(self, nums: List[int]) -> Optional[TreeNode]:
        if not nums:
            return None
        l,r = 0,len(nums)-1
        mid = (l+r) // 2
        root = TreeNode(nums[mid])
        root.left =  self.sortedArrayToBST(nums[0:mid])
        root.right = self.sortedArrayToBST(nums[mid+1:])
        return root
```

开始忘记了左闭右开了，哈哈哈，要注意