# 斐波那契数列

力扣509

## 思路

问的就是一个斐波那契数列的第n项是多少

斐波那契额的第1项是0，第二项是1，然后递推公式是 F（n） = F（n-1）+F（n-2）


```python 
class Solution:
    def fib(self, n: int) -> int:
        if n == 0:
            return 0
        if n == 1:
            return 1
        return self.fib(n-1) + self.fib(n-2)

```