# 反转二叉树
力扣226
## 初步思路
题目就是说把一个二叉树给反转过来，看了一下几个例子，思路还是有的

首先就是，对于一个节点，交换左右节点，然后继续递归左节点，递归右节点就行。

思路好像，代码怎么写还是要考虑一下

```python 
    def invertTree(self, root: Optional[TreeNode]) -> Optional[TreeNode]:
        if not root:
            return 
        root.left,root.right = root.right,root.left
        self.invertTree(root.left)
        self.invertTree(root.right)
        return root

```
不敢相信这么轻易的就写出来了，一遍过哈哈哈哈哈