# 反转字符串
力扣344
## 初步思路
就是把一个字符串反转过来，并且空间复杂度必须要是O1，一开始的想法是用类似冒泡那种，直接翻过来，但其实不用，直接双指针一头一尾直接开始遍历就行，very的简单
```python 
def reverseString(self, s: List[str]) -> None:
    l,r = 0,len(s)-1
    while l<r:
        s[l],s[r] = s[r],s[l]
        l +=1
        r -=1

```

